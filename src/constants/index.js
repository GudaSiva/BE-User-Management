//  we will write here : constant values

const { httpStatusCodes } = require("./http-status-code.constant");
const { httpResponses } = require("./http-response.constant");

module.exports = { httpStatusCodes, httpResponses };
